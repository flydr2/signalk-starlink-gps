syntax = "proto3";

package SpaceX.API.Device;

service Device {
  rpc GetStatus (Request) returns (Response) {}
}

message Request {
}

message Response {
  DishGetStatus dish_get_status = 1000;
}

message DishGetStatus {
  message Location {
    bool enabled = 1;
    double latitude = 2;
    double longitude = 3;
    double altitude_meters = 4;
    bool uncertainty_meters_valid = 5;
    double uncertainty_meters = 6;
    double gps_time_s = 7;
  }
  Location location = 10;
}
